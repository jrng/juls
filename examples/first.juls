exit :: (return_value: s32) {}

test :: (a: s64) -> s64
{
    b := a + a;
    return b;
}

main :: ()
{
    a := false;
    // b := 3.14;
    // c := "FizzBuzz";
    d : s64 = 42;
    // d : s8 = 42;
    e : u32 = 123;
    f : s8 = 23;

    g : s8 = 68;
    h : s8 = 70;

    // exit(d + f + 4);
    // exit(g - h);
    exit(cast(s32) test(d));
}
