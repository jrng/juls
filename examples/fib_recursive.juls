fib :: (a: u64) -> u64
{
    if (a <= 1) return a;

    return fib(a - 2) + fib(a - 1);
}

main :: ()
{
    // This gets truncated to a byte,
    // so the return value should be 225.
    exit(fib(47));
}
